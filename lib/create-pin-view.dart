import 'package:flutter/material.dart';

class CreatePinView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('last page to do')));
  }
}
